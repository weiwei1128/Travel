<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_gravity="center"
    android:background="#FFFFFF"
    android:orientation="vertical" >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="45dp" >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="周邊景點"
            android:id="@+id/mapHeader"
            android:textSize="22dp"
            android:gravity="center_vertical|center_horizontal"
            android:background="#5599FF"
            android:textColor="#FFFFFF" />

        <ImageView
            android:layout_width="30dp"
            android:layout_height="40dp"
            android:id="@+id/maps_backImg"
            android:src="@drawable/arrow_back_white"
            android:layout_gravity="center_vertical"
            android:background="@drawable/click_effect_general"
            android:layout_marginLeft="10dp" />
    </FrameLayout>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="1">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="景點地圖"
            android:id="@+id/spot_map_button"
            android:layout_weight="0.5"
            android:background="#ffffff"
            android:textSize="20dp" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="景點列表"
            android:id="@+id/spot_list_button"
            android:layout_weight="0.5"
            android:background="#ffffff"
            android:textSize="20dp" />
    </LinearLayout>

    <fragment
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MapsActivity"
        tools:ignore="MissingPrefix"
        map:cameraTargetLat="24.029548"
        map:cameraTargetLng="121.076745"
        map:cameraZoom="9" />
</LinearLayout>